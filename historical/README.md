# historical/

Superseded canonical artifacts. Preserved for audit trail. **Engines MUST NOT implement these.**

| file | superseded by | LIP record |
|---|---|---|
| `logline.receipt.v0.LIP-0003-superseded.md` + `.schema.json` | current `logline.receipt.v0` in `LogLine-Foundation/canon` (three-layer hashing) | `lips/LIP-0007-receipt-three-layer-hashing.md` |

The current canonical receipt format is in [`LogLine-Foundation/canon`](https://github.com/LogLine-Foundation/canon). Read these only to understand evolution, not as a target to implement.
